# method call execution path:
#  push params
#  push class
#  push block
#  tail call appropriate method
# in order that push block occurs at the correct place
# which is:
#  before the tail call to the method
# a dummy block push is inserted

